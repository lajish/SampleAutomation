pipeline {
	agent any
	stages{
		stage('Cloning') {
			steps {
				echo "This stage is for repository cloning"
				}
			}
		 stage('Execution') {
			steps {
				echo "This stage is for code execution"
				}
			}
	 }